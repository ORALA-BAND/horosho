<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>ALBUM RELEASE</title>
    <!-- Подключаем пиксельный шрифт -->
    <link href="https://googleapis.com" rel="stylesheet">
    <style>
        body, html { margin: 0; padding: 0; height: 100%; width: 100%; background: #050505; font-family: 'Press Start 2P', cursive; overflow: hidden; color: #fff; }

        /* ЗВЕЗДНЫЙ ФОН (Мерцающие розовые точки) */
        .stars {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: transparent;
            box-shadow: 10vw 20vh 2px #ffb6c1, 50vw 10vh 1px #ff69b4, 80vw 30vh 2px #ffb6c1, 20vw 70vh 1px #ff1493, 60vw 85vh 2px #ffb6c1, 90vw 60vh 1px #ff69b4, 30vw 40vh 2px #ffb6c1, 5vw 90vh 1px #ff1493;
            animation: twinkle 3s infinite ease-in-out;
            z-index: 0;
        }
        @keyframes twinkle { 0%, 100% { opacity: 0.3; } 50% { opacity: 1; } }

        .container { position: relative; z-index: 1; height: 100vh; display: flex; flex-direction: column; align-items: center; justify-content: center; }

        /* ПЕРЕВОРАЧИВАЮЩАЯСЯ ОБЛОЖКА */
        .cover-wrapper { perspective: 1000px; width: 280px; height: 280px; cursor: pointer; margin-bottom: 40px; }
        .cover-inner { position: relative; width: 100%; height: 100%; transition: transform 0.8s cubic-bezier(0.4, 0, 0.2, 1); transform-style: preserve-3d; }
        .cover-wrapper:active .cover-inner, .cover-wrapper.flipped .cover-inner { transform: rotateY(180deg); }

        .cover-front, .cover-back { position: absolute; width: 100%; height: 100%; backface-visibility: hidden; border-radius: 10px; box-shadow: 0 0 20px rgba(255, 105, 180, 0.4); border: 2px solid #ff69b4; }
        .cover-front { background: url('front.jpg') center/cover; }
        .cover-back { background: url('back.jpg') center/cover; transform: rotateY(180deg); }

        /* КНОПКИ */
        .controls { width: 90%; max-width: 500px; text-align: center; }
        .main-btn { font-size: 16px; color: #ff69b4; text-decoration: none; display: block; margin-bottom: 50px; cursor: pointer; transition: 0.2s; }
        .bottom-row { display: flex; justify-content: space-between; font-size: 8px; }
        .side-btn { color: #fff; text-decoration: none; cursor: pointer; transition: 0.2s; max-width: 40%; }
        
        .main-btn:hover, .side-btn:hover { color: #ff1493; transform: scale(1.1); }
    </style>
</head>
<body>

    <div class="stars"></div>

    <div class="container">
        <!-- Обложка (нажми, чтобы перевернуть) -->
        <div class="cover-wrapper" onclick="this.classList.toggle('flipped')">
            <div class="cover-inner">
                <div class="cover-front"></div>
                <div class="cover-back"></div>
            </div>
        </div>

        <div class="controls">
            <!-- Большая кнопка посередине -->
            <a onclick="downloadAlbum()" class="main-btn">хорошо horosho</a>
            
            <div class="bottom-row">
                <!-- Кнопки по бокам -->
                <a href="ССЫЛКА_НА_РЕКВИЗИТЫ_КАРТЫ" class="side-btn">поддержать релиз</a>
                <a href="ССЫЛКА_НА_PAYPAL" class="side-btn">support the release</a>
            </div>
        </div>
    </div>

    <script>
        function downloadAlbum() {
            if (confirm("Скачать альбом, download the album?")) {
                window.location.href = "ССЫЛКА_НА_ФАЙЛ_АЛЬБОМА"; 
            }
        }
    </script>

</body>
</html>
 